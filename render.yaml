services:
  - type: web
    name: portfolio-rag
    runtime: docker
    plan: free
    envVars:
      - key: LLM_PROVIDER
        value: huggingface
      - key: HF_TOKEN
        sync: false
      - key: CORS_ORIGINS
        value: "*"
