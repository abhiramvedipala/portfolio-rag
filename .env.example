LLM_PROVIDER=huggingface
HF_TOKEN=your_hf_token_here
OPENAI_API_KEY=optional_only_if_switching

# Comma-separated list of allowed origins for the /chat endpoint, e.g.
# https://abhiramreddy.dev. Defaults to "*" (any origin) if unset.
CORS_ORIGINS=*
